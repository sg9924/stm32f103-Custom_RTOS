1: INITIAL

2: BUG FIXES
    - task unblock bug fix
    - bug:
        - worked only for one blocked task
            - essentially handled only the head task in queue, for more than 1 blocked task, all of them are unblocked if one was to be unblocked
        - upon encountering one blocked task, it unblocks it and immediately returns without checking the rest of the queue for other blocked tasks
    - bug fixes:
        - logic fixed to handle both head and middle or last node in queue scenarios
        - return has been removed and placed outside the loop

3: BUG FIXES
    - bugs:
        - task quota reset worked only in certain scenarios
    - bug fixes:
        - fixed the loop logic for the task quota reset API
    - modified taskAdd_Idle
    - modified taskReset_Quota: resets quota for a specific task
    - new API: taskReset_QuotaAll
    - - RR and RR Weighted Scheduler improvements and bug fixes
4: TASK
    - Added return in taskAdd_Priority to return the TCB of the task being added which can be used later in the main program

5: QUEUE (Ready and Blocked Queues for Tasks)
    - Move the APIs from `rtos_queue` into `rtos_kernel`
    - `rtos_queue` will be used later for implementing Queues for Inter Task Communication
    - Cleanup kernel header
    - Remove rtos_queue header from rtos_task source
