1.: INITIAL
    - full rtos
    - 3 separate schedulers: round robin, round robin weighted and priority
    - scheduler type can be set in rtos_config.h
    - basic task blocking and unblocking via taskDelay() with block tick count
    - basic semaphore
    - no Mutex, IPC, Task Notifications yet

2: BUG FIX
    - Bug: incorrect size calculation in Startup code where we initialize .bss and copy .data to SRAM